# 计网课设编程练习题

每题独立一个 `.py` 文件，写完跑通即过关。答案在代码仓库里，写不出来再查。

---

## 第 1 题：`recv_exact()`

TCP 是流协议，单次 `recv(1024)` 可能只收到半个报文。实现一个函数，循环接收直到凑齐 n 字节。

```python
def recv_exact(sock, n: int) -> bytes:
    """精确接收 n 字节，收不够就继续等，连接断开抛异常"""
    # 你的代码
```

**测试**：不用跑网络。你写完之后我告诉你标准答案的关键行对比。

---

## 第 2 题：`split_file()`

给定文件内容、Lmin、Lmax、seed，将文件切成随机长度的块。验收必考题——老师给参数，你口算 N 和各块长度。

```python
import random

def split_file(data: bytes, lmin: int, lmax: int, seed: int) -> list:
    """
    返回 list[int]，每块长度。
    前 N-1 块长度在 [lmin, lmax]，最后一块 ≤ lmax 且可能小于 lmin。
    """
    # 你的代码

# 测试
result = split_file(b"A" * 520, 50, 100, 42)
print(f"N={len(result)}, 块长={result}, 总和={sum(result)}")
```

**目标**：能脱离代码口述分块逻辑——"固定种子→循环取随机值→剩余 ≤ Lmax 时收尾"。

---

## 第 3 题：`pack_header()` + `struct.unpack()`

Task1 协议头 5 字节 `[1B Type][4B Length]`，Task2 协议头 13 字节 `[1B Flags][4B Seq][4B Ack][4B Length]`。

```python
import struct

# 3a. Task1 打包 Initialization：Type=1，N 块数=9
def pack_init(n: int) -> bytes:
    pass

# 3b. Task1 解包 reverseRequest 头部：收到 5 字节头，拆出 Type 和 Length
def unpack_request_header(header: bytes) -> tuple:
    pass

# 3c. Task2 打包 DATA 报文：Flags=ACK(0x02)，Seq=5，长度 60，无 ACK
def pack_data(seq: int, length: int) -> bytes:
    pass

# 测试
print(pack_init(9))                        # 应输出 b'\x01\x00\x00\x00\x09'
print(unpack_request_header(pack_init(9))) # 应输出 (1, 9)
print(pack_data(5, 60))                    # 输出 13 字节的 DATA 头
print(len(pack_data(5, 60)))               # 应输出 13
```

---

## 第 4 题：GBN 窗口滑动

```python
# 初始状态
base = 3         # 窗口左边界（最老未确认）
next_seq = 8     # 下一个待发送序号
WINDOW = 5
TOTAL = 30

# 收到累积 ACK=6。画出 base 和 next_seq 的新值。
# 你的答案写在这里：
base_new = ?
next_seq_new = ?
```

---

## 第 5 题：RTO 自适应计算

```python
# 已知
estimated_rtt = 10.0   # 当前 EWMA 平滑值 (ms)
dev_rtt = 2.0          # 当前偏差 (ms)
sample_rtt = 15.0      # 刚测到的 RTT (ms)
alpha = 0.125
beta = 0.25

# 5a. 写出更新后的 estimated_rtt 和 dev_rtt（手算）
# 5b. 写出新的 TimeoutInterval（ms）
# 5c. 转成秒，限幅在 [0.05, 3.0]

# 你的答案：
# estimated_rtt_new = ?
# dev_rtt_new = ?
# timeout_ms = ?
# timeout_s = ?
```

---

## 第 6 题：三次握手之 StudentID

```python
STUDENT_ID_MASK = 0x5A3C

# 6a. Client 端：学号后4位=2913，计算 StudentID 字段
def compute_student_id(last4: int) -> int:
    pass

# 6b. Server 端：验证 StudentID 是否合法
def verify_student_id(received: int) -> bool:
    pass

# 测试
sid = compute_student_id(2913)
print(hex(sid))                # 应输出 0x515d
print(verify_student_id(sid))  # True
print(verify_student_id(0))    # False
```

---

## 第 7 题：SR 窗口滑动（升级版）

```python
# SR 发送方状态
base = 3
acked = [False] * 30
acked[0] = True; acked[1] = True; acked[2] = True  # 前三个已确认
acked[3] = True; acked[4] = False; acked[5] = True  # 4 没收，5 收了
acked[6] = True; acked[7] = False                    # 7 没收

# 窗口收到 seq=4 的 ACK（之前漏的补上了）
acked[4] = True

# 7a. base 从 3 开始往前推，能推到几？
# 7b. 为什么 SR 不能像 GBN 那样直接用 ack_num 设 base？

# 你的答案：
# base_new = ?
```

---

## 第 8 题：完整协议交互（综合）

用伪代码写出 Task1 Client 的完整交互流程，从读文件到写出反转文件。不需要写具体 socket 调用，写出每个阶段的步骤和对应的协议报文类型即可。

```text
1. 读文件
2. 调用 split_file() 分块
3. 连接 Server
4. 发送 Initialization（Type=1，含 N）
5. 收到 agree（Type=2）
6. 循环 N 次：
   a. 发送 reverseRequest（Type=3，含长度+数据）
   b. 收到 reverseAnswer（Type=4，含反转后数据）
   c. 打印第 x 块结果
7. 关闭连接
8. 逆序拼接所有反转块，写出文件
```

**你的任务**：把上面的文字流程补上每个步骤调用的函数名和关键参数。比如第 3 步写 `socket.connect((ip, port))`。
